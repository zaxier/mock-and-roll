[pytest]
testpaths = tests
pythonpath = src
markers =
    integration: marks tests as integration tests (may be slow)
    unit: marks tests as unit tests (fast)
    spark: marks tests as requiring Spark
    databricks: marks tests as requiring Databricks environment
